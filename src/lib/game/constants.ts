/** Drift radius for medium-level points (motion + classification disk). */
export const MEDIUM_POINT_RADIUS = 0.25;

/** Temporary: draw classification tolerance disks on the plot. Remove when done debugging. */
export const DEBUG_SHOW_CLASSIFICATION_DISKS = false;
